const json=(d,s=200)=>new Response(JSON.stringify(d),{status:s,headers:{'Content-Type':'application/json; charset=utf-8','Cache-Control':'no-store'}})
const enc=b=>btoa(String.fromCharCode(...new Uint8Array(b))).replace(/\+/g,'-').replace(/\//g,'_').replace(/=+$/,'')
function cfg(env,request){const origin=new URL(request.url).origin;return{url:String(env.CLOSET_SUPABASE_URL||env.NEXT_PUBLIC_SUPABASE_URL||'').replace(/\/$/,''),anon:String(env.CLOSET_SUPABASE_ANON_KEY||env.NEXT_PUBLIC_SUPABASE_ANON_KEY||''),service:String(env.CLOSET_SUPABASE_SERVICE_ROLE_KEY||env.SUPABASE_SERVICE_ROLE_KEY||''),clientId:String(env.MERCADOPAGO_CLIENT_ID||''),redirect:String(env.MERCADOPAGO_REDIRECT_URI||`${origin}/api/closet/mercadopago/callback`)}}
async function call(c,path,token,init={}){const r=await fetch(`${c.url}${path}`,{...init,headers:{apikey:c.service||c.anon,Authorization:`Bearer ${token}`,'Content-Type':'application/json',...(init.headers||{})}}),t=await r.text();let d=null;try{d=t?JSON.parse(t):null}catch{d=t}if(!r.ok)throw Error(d?.message||d?.error_description||d?.error||`Supabase ${r.status}`);return d}
export async function onRequestPost({request,env}){try{const c=cfg(env,request);if(!c.url||!c.anon||!c.service||!c.clientId)return json({message:'OAuth Mercado Pago ainda não configurado no deploy.'},503);const auth=request.headers.get('Authorization')||'',userToken=auth.startsWith('Bearer ')?auth.slice(7):'';if(!userToken)return json({message:'Sessão obrigatória.'},401);const user=await call(c,'/auth/v1/user',userToken),body=await request.json(),sellerId=String(body?.seller_id||'');if(!sellerId)return json({message:'seller_id obrigatório.'},400);const members=await call(c,`/rest/v1/closet_seller_users?select=id&seller_id=eq.${encodeURIComponent(sellerId)}&user_id=eq.${encodeURIComponent(user.id)}&active=eq.true&limit=1`,c.service);if(!members?.[0])return json({message:'Seller não pertence a esta conta.'},403);const bytes=crypto.getRandomValues(new Uint8Array(48)),verifier=enc(bytes),challenge=enc(await crypto.subtle.digest('SHA-256',new TextEncoder().encode(verifier))),state=crypto.randomUUID().replaceAll('-','')+crypto.randomUUID().replaceAll('-','');await call(c,'/rest/v1/rpc/closet_create_oauth_state',c.service,{method:'POST',body:JSON.stringify({p_seller_id:sellerId,p_provider:'mercadopago',p_state:state,p_verifier:verifier,p_redirect_uri:c.redirect,p_ttl_minutes:10})});const u=new URL('https://auth.mercadopago.com/authorization');u.searchParams.set('client_id',c.clientId);u.searchParams.set('response_type','code');u.searchParams.set('platform_id','mp');u.searchParams.set('state',state);u.searchParams.set('redirect_uri',c.redirect);u.searchParams.set('code_challenge',challenge);u.searchParams.set('code_challenge_method','S256');return json({ok:true,authorization_url:u.toString()})}catch(e){return json({message:e?.message||'Não consegui iniciar a conexão.'},500)}}
