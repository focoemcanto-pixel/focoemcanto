const json=(d,s=200)=>new Response(JSON.stringify(d),{status:s,headers:{'Content-Type':'application/json; charset=utf-8','Cache-Control':'no-store'}});
function cfg(e){return{url:String(e.CLOSET_SUPABASE_URL||e.NEXT_PUBLIC_SUPABASE_URL||'').replace(/\/$/,''),service:String(e.CLOSET_SUPABASE_SERVICE_ROLE_KEY||e.SUPABASE_SERVICE_ROLE_KEY||''),secret:String(e.CLOSET_CRON_SECRET||'')}}
async function sb(c,path,init={}){const r=await fetch(`${c.url}${path}`,{...init,headers:{apikey:c.service,Authorization:`Bearer ${c.service}`,'Content-Type':'application/json',...(init.headers||{})}}),t=await r.text();let d=null;try{d=t?JSON.parse(t):null}catch{d=t}if(!r.ok)throw Error(d?.message||d?.error||`Supabase ${r.status}`);return d}
export async function onRequestPost({request,env}){const c=cfg(env),auth=request.headers.get('Authorization')||'';if(!c.url||!c.service||!c.secret)return json({message:'Rotina não configurada.'},503);if(auth!==`Bearer ${c.secret}`)return json({message:'Não autorizado.'},401);try{const changed=await sb(c,'/rest/v1/rpc/closet_refresh_payout_eligibility',{method:'POST',body:JSON.stringify({p_order_id:null})});return json({ok:true,changed:Number(changed||0),checked_at:new Date().toISOString()})}catch(e){return json({message:e?.message||'Falha ao atualizar repasses.'},500)}}