import { isAdminAuthenticated } from '../../../_lib/admin-auth.js'
const json=(d,s=200)=>new Response(JSON.stringify(d),{status:s,headers:{'Content-Type':'application/json; charset=utf-8','Cache-Control':'no-store'}})
export async function onRequestGet({request,env,params}){if(!(await isAdminAuthenticated(request,env)))return json({message:'Não autorizado.'},401);const id=String(params.id||''),raw=await env.FOCO_LINKS.get(`divisoes:submission:${id}`);if(!raw)return json({message:'Envio não encontrado.'},404);const r=JSON.parse(raw),obj=await env.FOCO_MEDIA.get(r.storageKey);if(!obj)return json({message:'Arquivo não encontrado.'},404);const h=new Headers();obj.writeHttpMetadata(h);h.set('Cache-Control','private, no-store');h.set('Content-Disposition',`inline; filename="${String(r.fileName||id).replace(/["\r\n]/g,'')}"`);return new Response(obj.body,{headers:h})}
