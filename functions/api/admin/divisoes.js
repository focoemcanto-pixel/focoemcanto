import { isAdminAuthenticated } from '../../_lib/admin-auth.js'
const json=(d,s=200)=>new Response(JSON.stringify(d),{status:s,headers:{'Content-Type':'application/json; charset=utf-8','Cache-Control':'no-store'}}),statuses=new Set(['received','shortlisted','recorded','published','archived'])
export async function onRequestGet({request,env}){if(!(await isAdminAuthenticated(request,env)))return json({message:'Não autorizado.'},401);const listed=await env.FOCO_LINKS.list({prefix:'divisoes:submission:',limit:100}),rows=(await Promise.all(listed.keys.map(async k=>{try{return JSON.parse(await env.FOCO_LINKS.get(k.name))}catch{return null}}))).filter(x=>x&&x.status!=='pending_upload').sort((a,b)=>String(b.createdAt).localeCompare(String(a.createdAt))).map(({uploadToken,...x})=>x);return json({ok:true,rows})}
export async function onRequestPatch({request,env}){if(!(await isAdminAuthenticated(request,env)))return json({message:'Não autorizado.'},401);let b;try{b=await request.json()}catch{return json({message:'Dados inválidos.'},400)}const id=String(b.id||''),status=String(b.status||''),key=`divisoes:submission:${id}`,raw=await env.FOCO_LINKS.get(key);if(!raw||!statuses.has(status))return json({message:'Atualização inválida.'},422);const r=JSON.parse(raw);r.status=status;r.updatedAt=new Date().toISOString();await env.FOCO_LINKS.put(key,JSON.stringify(r));return json({ok:true})}
