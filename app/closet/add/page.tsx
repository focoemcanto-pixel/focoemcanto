'use client';
import {ChangeEvent,useEffect,useRef,useState} from 'react';
import {restoreClosetSession,saveClosetItem,type ClosetSession} from '../supabase';
import {loadAiWallet,type AiWallet} from '../aiCredits';
import styles from './add.module.css';

type Category='Blusas'|'Calças'|'Vestidos'|'Calçados'|'Bolsas'|'Acessórios';
const categories:Category[]=['Blusas','Calças','Vestidos','Calçados','Bolsas','Acessórios'];
async function imageToDataUrl(file:File){const raw=await new Promise<string>((resolve,reject)=>{const r=new FileReader();r.onload=()=>resolve(String(r.result));r.onerror=reject;r.readAsDataURL(file)});const img=await new Promise<HTMLImageElement>((resolve,reject)=>{const el=new Image();el.onload=()=>resolve(el);el.onerror=reject;el.src=raw});const max=1400,scale=Math.min(1,max/Math.max(img.width,img.height)),c=document.createElement('canvas');c.width=Math.max(1,Math.round(img.width*scale));c.height=Math.max(1,Math.round(img.height*scale));c.getContext('2d')?.drawImage(img,0,0,c.width,c.height);return c.toDataURL('image/jpeg',.88)}
export default function AddPiecePage(){const [session,setSession]=useState<ClosetSession|null>(null),[wallet,setWallet]=useState<AiWallet|null>(null),[mode,setMode]=useState<'choose'|'manual'>('choose'),[image,setImage]=useState(''),[name,setName]=useState(''),[category,setCategory]=useState<Category>('Blusas'),[color,setColor]=useState(''),[subcategory,setSubcategory]=useState(''),[saving,setSaving]=useState(false),[message,setMessage]=useState('');const camera=useRef<HTMLInputElement>(null),gallery=useRef<HTMLInputElement>(null);useEffect(()=>{(async()=>{const s=await restoreClosetSession();setSession(s);if(s)setWallet(await loadAiWallet(s))})()},[]);async function receive(e:ChangeEvent<HTMLInputElement>){const f=e.target.files?.[0];if(!f)return;try{setImage(await imageToDataUrl(f));setMode('manual')}finally{e.target.value=''}}async function save(){if(!image)return;if(!session){setMessage('Entre na sua conta antes de guardar a peça.');return}if(!name.trim()){setMessage('Dê um nome para a peça.');return}setSaving(true);setMessage('');try{await saveClosetItem(session,{name:name.trim(),category,color:color.trim(),source:{subcategory:subcategory.trim(),pattern:null,style:null,confidence:1,visibility:1,manual:true},catalogImage:image,originalImage:image});location.href='/closet/wardrobe'}catch(e:any){setMessage(e?.message||'Não consegui guardar a peça.')}finally{setSaving(false)}}return <main className={styles.page}><header><button onClick={()=>history.back()}>‹</button><div><span>ADICIONAR PEÇA</span><strong>Novo item</strong></div><button onClick={()=>location.href='/closet'}>⌂</button></header>{mode==='choose'?<section className={styles.choose}><span>VOCÊ ESCOLHE</span><h1>Cadastre grátis ou deixe a IA preparar.</h1><p>A foto normal funciona em todo o Closet. A IA é opcional e só será usada quando você pedir.</p><button className={styles.freeCard} onClick={()=>setMode('manual')}><b>GRÁTIS</b><strong>Cadastrar pela foto</strong><small>Use a própria foto da peça. Sem scanner, sem reconstrução e sem consumo de IA.</small><i>Continuar →</i></button><button className={styles.aiCard} onClick={()=>location.href='/closet/credits'}><b>✨ CLOSET AI</b><strong>Preparar automaticamente</strong><small>Scanner inteligente, separação de peças e versão de catálogo. Usa créditos quando ativado.</small><i>{wallet?`${wallet.balance} créditos disponíveis`:'Ver créditos'} →</i></button><div className={styles.note}>Nenhuma função de IA é executada automaticamente ao escolher uma foto.</div></section>:<section className={styles.manual}><div className={styles.title}><span>CADASTRO GRATUITO</span><h1>Sua foto, sua peça.</h1><p>Enquadre a peça do jeito que quiser. Você poderá melhorar com Closet AI depois, se desejar.</p></div>{!image?<div className={styles.capture}><button onClick={()=>camera.current?.click()}><span>◎</span><strong>Tirar foto</strong><small>usar câmera</small></button><button onClick={()=>gallery.current?.click()}><span>▧</span><strong>Escolher da galeria</strong><small>usar foto existente</small></button></div>:<><div className={styles.preview}><img src={image} alt="Peça escolhida"/><button onClick={()=>setImage('')}>Trocar foto</button></div><div className={styles.form}><label>Nome<input value={name} onChange={e=>setName(e.target.value)} placeholder="Ex.: Camisa off-white"/></label><label>Categoria<select value={category} onChange={e=>setCategory(e.target.value as Category)}>{categories.map(c=><option key={c}>{c}</option>)}</select></label><label>Subcategoria <small>opcional</small><input value={subcategory} onChange={e=>setSubcategory(e.target.value)} placeholder="Ex.: camisa social, tênis, chino"/></label><label>Cor principal <small>opcional</small><input value={color} onChange={e=>setColor(e.target.value)} placeholder="Ex.: off-white"/></label></div>{message&&<div className={styles.message}>{message}</div>}<button className={styles.save} disabled={saving} onClick={save}>{saving?'Guardando…':'Guardar no meu closet'}</button><button className={styles.aiLater} onClick={()=>location.href='/closet/credits'}>✨ Melhorar com Closet AI depois</button></>}</section>}<input ref={camera} type="file" accept="image/*" capture="environment" hidden onChange={receive}/><input ref={gallery} type="file" accept="image/*" hidden onChange={receive}/></main>}
