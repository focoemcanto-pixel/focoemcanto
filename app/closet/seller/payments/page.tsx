'use client';
import {useEffect,useState} from 'react';
import {restoreClosetSession,type ClosetSession} from '../../supabase';
import {loadSeller} from '../../marketplaceV2';
import styles from '../seller.module.css';
export default function SellerPaymentsPage(){const [session,setSession]=useState<ClosetSession|null>(null),[seller,setSeller]=useState<any>(null),[state,setState]=useState<any>(null),[loading,setLoading]=useState(true),[busy,setBusy]=useState(false),[error,setError]=useState('');async function sync(s:ClosetSession,sellerId:string){const r=await fetch('/api/closet/stripe/status',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${s.access_token}`},body:JSON.stringify({seller_id:sellerId})}),d=await r.json();if(!r.ok)throw Error(d.message||'Não consegui consultar o financeiro.');setState(d)}useEffect(()=>{let alive=true;(async()=>{try{const s=await restoreClosetSession();if(!alive)return;setSession(s);if(!s)return;const sl=await loadSeller(s);if(!alive)return;setSeller(sl);if(sl)await sync(s,sl.id)}catch(e:any){if(alive)setError(e?.message||'Não consegui abrir esta etapa.')}finally{if(alive)setLoading(false)}})();return()=>{alive=false}},[]);async function connect(){if(!session||!seller)return;setBusy(true);setError('');try{const r=await fetch('/api/closet/stripe/connect',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${session.access_token}`},body:JSON.stringify({seller_id:seller.id})}),d=await r.json();if(!r.ok)throw Error(d.message||'Não consegui iniciar o onboarding.');location.href=d.onboarding_url}catch(e:any){setError(e?.message||'Não consegui conectar a conta.');setBusy(false)}}if(loading)return <main className={styles.page}><div className={styles.loading}>Abrindo financeiro…</div></main>;return <main className={styles.page}><header className={styles.header}><button onClick={()=>location.href='/closet/seller'}>‹</button><div><span>CLOSET SELLER</span><strong>Pagamentos</strong></div><button onClick={()=>location.href='/closet'}>⌂</button></header><div className={styles.wrap}><section className={styles.panel}><span className={styles.eyebrow}>CONTA DE REPASSE</span><h2>Conecte sua operação financeira.</h2><p>O Closet usa uma conta conectada para verificar dados de pagamento e, quando as vendas diretas forem ativadas, controlar o fluxo entre compra, proteção e repasse. O onboarding financeiro é feito pelo provedor de pagamentos.</p>{!session||!seller?<div className={styles.empty}><strong>Seller não encontrado.</strong><span>Crie seu cadastro de vendedor primeiro.</span></div>:<div className={styles.cards}><article className={styles.card}><div className={styles.cardHead}><div><small>STRIPE CONNECT</small><h3>{state?.connected?'Conta pronta':state?.status==='pending'?'Cadastro financeiro pendente':'Ainda não conectado'}</h3></div><span className={`${styles.status} ${state?.connected?styles.ok:styles.warn}`}>{state?.connected?'ativo':'pendente'}</span></div><p>{state?.connected?'Transferências habilitadas. Isso não ativa vendas sozinho: a feature flag do Marketplace continua protegendo o checkout.':'Conclua os dados solicitados pelo provedor para habilitar transferências.'}</p>{state?.requirements_due?.length>0&&<div className={styles.error}>{state.requirements_due.length} requisito(s) financeiro(s) ainda pendente(s).</div>}<div className={styles.actions}><button className={styles.primary} disabled={busy} onClick={connect}>{busy?'Abrindo…':state?.connected?'Revisar cadastro financeiro':'Conectar conta de pagamento'}</button>{state&&<button className={styles.secondary} onClick={()=>session&&seller&&sync(session,seller.id)}>Atualizar status</button>}</div></article><article className={styles.card}><small>SEGURANÇA DO REPASSE</small><h3>Venda não significa repasse imediato.</h3><p>A arquitetura separa pagamento, pedido, entrega, contestação e payout. A liberação ao seller só acontece quando a política de proteção do pedido permitir.</p></article></div>}{error&&<div className={styles.error}>{error}</div>}<p style={{fontSize:11,color:'#7c6b5c',lineHeight:1.55}}>As vendas diretas permanecem desativadas até credenciais, webhooks, logística, políticas jurídicas e conciliação estarem validados em produção.</p></section></div></main>}
