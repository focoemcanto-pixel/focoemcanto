'use client';
import {useEffect,useState} from 'react';
import {restoreClosetSession,type ClosetSession} from '../../supabase';
import {loadSellerOrderDetail} from '../../marketplaceSellerOps';
import styles from '../seller.module.css';
const labels:Record<string,string>={pending_payment:'Aguardando pagamento',paid:'Pago',awaiting_shipment:'Preparar envio',shipped:'Enviado',in_transit:'Em trânsito',out_for_delivery:'Saiu para entrega',delivered:'Entregue',completed:'Concluído',cancelled:'Cancelado',disputed:'Em contestação',return_requested:'Devolução solicitada',return_in_transit:'Devolução em trânsito',returned:'Devolvido',partially_refunded:'Estorno parcial',refunded:'Estornado'};
function money(v:any){return new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(Number(v||0))}
export default function SellerOrderDetail(){const [session,setSession]=useState<ClosetSession|null>(null),[detail,setDetail]=useState<any>(null),[loading,setLoading]=useState(true),[error,setError]=useState('');const id=typeof window!=='undefined'?new URLSearchParams(location.search).get('id')||'':'';useEffect(()=>{let alive=true;(async()=>{try{const s=await restoreClosetSession();if(!alive)return;setSession(s);if(s&&id){const d=await loadSellerOrderDetail(s,id);if(alive)setDetail(d)}}catch(e:any){if(alive)setError(e?.message||'Não consegui abrir este pedido.')}finally{if(alive)setLoading(false)}})();return()=>{alive=false}},[id]);if(loading)return <main className={styles.page}><div className={styles.loading}>Abrindo pedido…</div></main>;const o=detail?.order;if(!session||!o)return <main className={styles.page}><div className={styles.empty}><strong>Pedido não encontrado.</strong><span>{error||'Entre com a conta do seller responsável.'}</span><button className={styles.primary} onClick={()=>location.href='/closet/seller/orders'}>Voltar</button></div></main>;const addr=o.shipping_address_snapshot||{};return <main className={styles.page}><header className={styles.header}><button onClick={()=>location.href='/closet/seller/orders'}>‹</button><div><span>PEDIDO SELLER</span><strong>CL-{String(o.order_number).padStart(6,'0')}</strong></div><button onClick={()=>location.href='/closet'}>⌂</button></header><div className={styles.wrap}><section className={styles.hero}><span className={styles.eyebrow}>{labels[o.status]||o.status}</span><h1>{money(o.total_amount)}</h1><p>Pedido criado em {new Date(o.created_at).toLocaleString('pt-BR')}. Estados financeiros e logísticos são registrados por eventos, não por edição manual.</p></section>{error&&<div className={styles.error}>{error}</div>}<section className={styles.panel}><span className={styles.eyebrow}>ITENS</span><h2>Conteúdo do pedido</h2>{detail.items?.map((it:any)=><div className={styles.row} key={it.id}><span>{it.quantity} × {money(it.unit_price)}</span><strong>{money(it.subtotal)}</strong></div>)}</section><section className={styles.panel}><span className={styles.eyebrow}>DESTINO</span><h2>Entrega</h2>{addr.street?<p>{addr.recipient_name&&<><strong>{addr.recipient_name}</strong><br/></>}{addr.street}, {addr.number||'s/n'}{addr.complement?` · ${addr.complement}`:''}<br/>{addr.district?`${addr.district} · `:''}{addr.city} · {addr.state}<br/>{addr.postal_code||addr.zip||''}</p>:<p>Endereço não disponível neste pedido.</p>}{detail.shipments?.length?<div className={styles.cards}>{detail.shipments.map((s:any)=><article className={styles.card} key={s.id}><div className={styles.cardHead}><div><small>{s.provider}</small><h3>{labels[s.status]||s.status}</h3></div><span className={styles.status}>{s.tracking_code||'sem rastreio'}</span></div>{s.estimated_delivery_at&&<p>Previsão: {new Date(s.estimated_delivery_at).toLocaleDateString('pt-BR')}</p>}</article>)}</div>:<div className={styles.empty}><strong>Aguardando logística.</strong><span>Quando o provedor gerar etiqueta/postagem, o tracking será vinculado automaticamente a este pedido.</span></div>}</section><section className={styles.panel}><span className={styles.eyebrow}>FINANCEIRO</span><h2>Composição</h2><div className={styles.row}><span>Produtos</span><strong>{money(o.products_amount)}</strong></div><div className={styles.row}><span>Frete</span><strong>{money(o.shipping_amount)}</strong></div><div className={styles.row}><span>Comissão Closet</span><strong>- {money(o.commission_amount)}</strong></div><div className={styles.row}><span>Processamento</span><strong>- {money(o.processing_fee_amount)}</strong></div><div className={styles.row}><span>Líquido seller</span><strong>{money(o.seller_net_amount)}</strong></div></section></div></main>}
