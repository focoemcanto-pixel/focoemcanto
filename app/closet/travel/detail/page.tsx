'use client';
import {useEffect,useMemo,useState} from 'react';
import {loadClosetItems,restoreClosetSession,type ClosetSession} from '../../supabase';
import {loadTrip,loadTripMoments,loadTripMomentItems,loadTripPacking,setTripPackingState,type Trip,type TripMoment,type TripMomentItem,type TripPackingItem} from '../../travelStore';
import styles from './trip.module.css';
type ClosetItem={id:string;name:string;category:string;color?:string|null;subcategory?:string|null;image?:string};type Tab='route'|'looks'|'packing';
export default function TripPage(){const [id,setId]=useState(''),[session,setSession]=useState<ClosetSession|null>(null),[trip,setTrip]=useState<Trip|null>(null),[moments,setMoments]=useState<TripMoment[]>([]),[momentItems,setMomentItems]=useState<TripMomentItem[]>([]),[packing,setPacking]=useState<TripPackingItem[]>([]),[closet,setCloset]=useState<ClosetItem[]>([]),[tab,setTab]=useState<Tab>('route'),[expanded,setExpanded]=useState<string|null>(null),[loading,setLoading]=useState(true),[error,setError]=useState('');useEffect(()=>{const tripId=new URLSearchParams(window.location.search).get('id')||'';setId(tripId);(async()=>{if(!tripId){setError('Viagem não identificada.');setLoading(false);return}const s=await restoreClosetSession();setSession(s);if(!s){setError('Entre na sua conta para abrir esta viagem.');setLoading(false);return}const [t,m,mi,p,c]=await Promise.all([loadTrip(s,tripId),loadTripMoments(s,tripId),loadTripMomentItems(s,tripId),loadTripPacking(s,tripId),loadClosetItems(s)]);if(!t){setError('Não encontrei esta viagem.');setLoading(false);return}setTrip(t);setMoments(m);setMomentItems(mi);setPacking(p);setCloset(c as ClosetItem[]);setLoading(false)})().catch(e=>{setError(e?.message||'Não consegui abrir a viagem.');setLoading(false)})},[]);
const itemMap=useMemo(()=>new Map(closet.map(x=>[String(x.id),x])),[closet]);const looks=useMemo(()=>moments.map(m=>{const links=momentItems.filter(x=>x.moment_id===m.id),items=links.map(x=>itemMap.get(String(x.item_id))).filter(Boolean) as ClosetItem[];return{moment:m,links,items}}).filter(x=>x.items.length),[moments,momentItems,itemMap]);const packedCount=packing.filter(x=>x.packed).length;async function togglePacked(row:TripPackingItem){if(!session||!trip)return;const next=!row.packed;setPacking(v=>v.map(x=>x.item_id===row.item_id?{...x,packed:next}:x));try{await setTripPackingState(session,trip.id,row.item_id,next)}catch{setPacking(v=>v.map(x=>x.item_id===row.item_id?{...x,packed:!next}:x))}}
if(loading)return <main className={styles.page}><div className={styles.center}>Abrindo sua viagem…</div></main>;if(error||!trip)return <main className={styles.page}><div className={styles.center}><h1>Não consegui abrir.</h1><p>{error}</p><button onClick={()=>location.href='/closet/travel'}>Voltar às viagens</button></div></main>;
return <main className={styles.page}><header><button onClick={()=>location.href='/closet/travel'}>‹</button><div><span>TRAVEL CLOSET</span><strong>{trip.name}</strong></div><button onClick={()=>location.href='/closet'}>⌂</button></header><section className={styles.hero}><span>{trip.destination.toUpperCase()}</span><h1>{trip.name}</h1><p>{new Date(`${trip.start_date}T12:00:00`).toLocaleDateString('pt-BR')} → {new Date(`${trip.end_date}T12:00:00`).toLocaleDateString('pt-BR')}</p></section><section className={styles.summaryRail}><button onClick={()=>setTab('route')}><span>ROTEIRO</span><strong>{moments.length}</strong><small>{moments.length===1?'momento':'momentos'}</small></button><button onClick={()=>setTab('looks')}><span>LOOKS</span><strong>{looks.length}</strong><small>{looks.length===1?'pronto':'prontos'}</small></button><button onClick={()=>setTab('packing')}><span>MALA</span><strong>{packedCount}/{packing.length}</strong><small>separadas</small></button></section><nav className={styles.tabs}><button className={tab==='route'?styles.active:''} onClick={()=>setTab('route')}>Roteiro</button><button className={tab==='looks'?styles.active:''} onClick={()=>setTab('looks')}>Looks</button><button className={tab==='packing'?styles.active:''} onClick={()=>setTab('packing')}>Mala</button></nav>
{tab==='route'&&<section className={styles.route}><div className={styles.routeHead}><div><span>SEUS MOMENTOS</span><h2>Roteiro da viagem</h2></div><button onClick={()=>location.href='/closet/travel'}>Editar viagem</button></div>{moments.length?moments.map((m,i)=>{const d=m.starts_at?new Date(m.starts_at):null,ready=momentItems.some(x=>x.moment_id===m.id);return <article key={m.id}><b>{String(i+1).padStart(2,'0')}</b><div><small>{d?`${d.toLocaleDateString('pt-BR')} · ${d.toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'})}`:'Sem horário definido'}</small><strong>{m.title}</strong><p>{m.location_name||trip.destination}{m.environment?` · ${m.environment==='indoor'?'ambiente interno':m.environment==='outdoor'?'ao ar livre':'ambiente misto'}`:''}</p>{ready&&<em className={styles.ready}>✓ look definido</em>}<div className={styles.actions}><button onClick={()=>location.href=`/closet/travel/look/manual/?trip=${trip.id}&moment=${m.id}`}>Montar manualmente</button><button onClick={()=>location.href=`/closet/travel/look/?trip=${trip.id}&moment=${m.id}`}>✦ {ready?'Rever com Stylist':'Stylist sugerir'}</button></div></div></article>}):<div className={styles.empty}>Nenhum momento foi adicionado ainda.</div>}</section>}
{tab==='looks'&&<section className={styles.looksSection}><div className={styles.sectionTitle}><span>LOOKBOOK DA VIAGEM</span><h2>Seus looks, momento a momento.</h2><p>Toque em um card para ver as peças e ajustar a combinação.</p></div>{looks.length?<div className={styles.lookList}>{looks.map(({moment,items,links})=>{const d=moment.starts_at?new Date(moment.starts_at):null,open=expanded===moment.id;return <article className={`${styles.lookCard} ${open?styles.lookCardOpen:''}`} key={moment.id}><button className={styles.lookCardMain} onClick={()=>setExpanded(open?null:moment.id)}><div className={styles.lookThumbs}>{items.slice(0,4).map((p,i)=><span key={p.id} style={{zIndex:5-i}}><img src={p.image} alt=""/></span>)}</div><div className={styles.lookCopy}><small>{d?`${d.toLocaleDateString('pt-BR')} · ${d.toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'})}`:'LOOK SALVO'}</small><strong>{moment.title}</strong><p>{moment.location_name||trip.destination} · {items.length} peças</p></div><b>{open?'−':'+'}</b></button>{open&&<div className={styles.lookDetails}><div className={styles.pieceStrip}>{items.map(p=><div key={p.id}><img src={p.image} alt={p.name}/><small>{p.name}</small></div>)}</div><div className={styles.lookMeta}><span>{links[0]?.source==='manual'?'MONTADO POR VOCÊ':'SUGERIDO PELO STYLIST'}</span><p>Este look está ligado ao momento “{moment.title}” e já entra automaticamente na mala da viagem.</p></div><div className={styles.lookActions}><button onClick={()=>location.href=`/closet/travel/look/manual/?trip=${trip.id}&moment=${moment.id}`}>Editar peças</button><button onClick={()=>location.href=`/closet/travel/look/?trip=${trip.id}&moment=${moment.id}`}>✦ Nova sugestão</button></div></div>}</article>})}</div>:<div className={styles.premiumEmpty}><span>♡</span><strong>Seu lookbook ainda está vazio.</strong><p>Volte ao roteiro e defina o primeiro look. Ele aparecerá aqui como parte da viagem.</p><button onClick={()=>setTab('route')}>Ir para o roteiro</button></div>}</section>}
{tab==='packing'&&<section className={styles.packingSection}><div className={styles.sectionTitle}><span>MALA INTELIGENTE</span><h2>{packing.length?`${packedCount} de ${packing.length} peças separadas.`:'A mala nasce dos seus looks.'}</h2><p>{packing.length?'Marque enquanto arruma. Peças repetidas em vários looks aparecem apenas uma vez.':'Salve looks no roteiro e o Closet consolida automaticamente o que você precisa levar.'}</p></div>{packing.length?<div className={styles.packingList}>{packing.map(row=>{const p=itemMap.get(String(row.item_id));if(!p)return null;return <button key={row.item_id} className={`${styles.packItem} ${row.packed?styles.packDone:''}`} onClick={()=>togglePacked(row)}><span className={styles.check}>{row.packed?'✓':''}</span><img src={p.image} alt={p.name}/><div><strong>{p.name}</strong><small>{[p.category,p.color].filter(Boolean).join(' · ')}</small></div>{row.purchased_for_trip&&<em>nova</em>}</button>})}</div>:<div className={styles.premiumEmpty}><span>▦</span><strong>Nada para colocar na mala ainda.</strong><p>Assim que você salvar os looks dos momentos da viagem, eu organizo as peças aqui.</p><button onClick={()=>setTab('route')}>Planejar looks</button></div>}</section>}</main>}
