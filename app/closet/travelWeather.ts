'use client';
import type {ClosetSession} from './supabase';
import {updateMoment,type Trip,type TripMoment} from './travelStore';
export type TravelWeather={source:'forecast'|'historical'|'historical_typical';sourceLabel:string;tempMin:number|null;tempMax:number|null;rainChance:number|null;weatherCode:number|null;windMax:number|null;thermalNeed:number;note:string;location:{name:string;admin1?:string|null;country?:string|null;latitude:number;longitude:number;timezone?:string|null}};
export async function fetchTravelWeather(trip:Trip,moment:TripMoment):Promise<TravelWeather>{if(!moment.starts_at)throw new Error('Defina a data deste momento para consultar o clima.');const q=new URLSearchParams({destination:trip.destination,place:moment.location_name||trip.destination,date:new Date(moment.starts_at).toISOString().slice(0,10)}),r=await fetch(`/api/closet/travel-weather?${q.toString()}`),d=await r.json();if(!r.ok||!d?.ok)throw new Error(d?.message||'Não consegui consultar o clima.');return d as TravelWeather}
export async function refreshMomentWeather(session:ClosetSession,trip:Trip,moment:TripMoment){const w=await fetchTravelWeather(trip,moment),metadata={...(moment.metadata||{}),weather_note:w.note,weather_checked_at:new Date().toISOString(),weather_code:w.weatherCode,wind_max:w.windMax,weather_location:w.location,weather_context_version:1};const updated=await updateMoment(session,moment.id,{expected_temp_min:w.tempMin,expected_temp_max:w.tempMax,rain_probability:w.rainChance,weather_source:w.sourceLabel,thermal_need:w.thermalNeed,metadata});return{moment:updated,weather:w}}
