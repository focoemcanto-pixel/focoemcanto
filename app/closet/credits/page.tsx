'use client';
import {useEffect,useState} from 'react';import {restoreClosetSession,type ClosetSession} from '../supabase';import {loadAiWallet,loadAiCreditHistory,type AiWallet,type AiCreditEntry} from '../aiCredits';import styles from './credits.module.css';
function label(x:AiCreditEntry){if(x.kind==='partner_reward')return'Benefício de parceiro';if(x.kind==='ai_usage')return'Uso do Closet AI';if(x.kind==='purchase')return'Créditos adicionados';if(x.kind==='promo')return'Crédito promocional';if(x.kind==='referral')return'Indicação';if(x.kind==='refund')return'Estorno automático';return x.kind}
function operation(x:AiCreditEntry){if(x.operation==='scan')return'Scanner inteligente';if(x.operation==='catalogize')return'Melhorar peça com IA';if(x.operation==='try-on')return'Experimentar look com IA';if(x.operation?.startsWith('refund:'))return'Crédito devolvido após falha';return x.operation||new Date(x.created_at).toLocaleDateString('pt-BR')}
export default function CreditsPage(){const [session,setSession]=useState<ClosetSession|null>(null),[wallet,setWallet]=useState<AiWallet|null>(null),[entries,setEntries]=useState<AiCreditEntry[]>([]),[loading,setLoading]=useState(true);useEffect(()=>{(async()=>{const s=await restoreClosetSession();setSession(s);if(s){setWallet(await loadAiWallet(s));setEntries(await loadAiCreditHistory(s))}setLoading(false)})()},[]);return <main className={styles.page}><header><button onClick={()=>window.history.back()}>‹</button><div><span>CLOSET AI</span><strong>Meus créditos</strong></div><button onClick={()=>location.href='/closet'}>⌂</button></header><section className={styles.hero}><span>CARTEIRA DE BENEFÍCIOS</span><h1>{loading?'…':wallet?.balance||0}</h1><strong>créditos disponíveis</strong><p>Use créditos apenas quando quiser recursos especiais de IA. Seu closet continua funcionando normalmente sem eles.</p></section><section className={styles.actions}><article><b>✨</b><div><strong>Ganhar créditos</strong><p>Compras em parceiros, campanhas, indicações e benefícios poderão reabastecer sua carteira.</p></div></article><article><b>＋</b><div><strong>Adicionar créditos</strong><p>Quando a compra direta estiver disponível, você poderá completar o saldo sempre que quiser.</p></div></article></section><section className={styles.costs}><span>COMO FUNCIONA</span><h2>Você decide quando usar IA.</h2><div><p><span><strong>Scanner inteligente</strong><small>Identifica as peças de uma foto. Não reconstrói automaticamente.</small></span><b>1 crédito</b></p><p><span><strong>Melhorar uma peça</strong><small>Cria uma versão limpa de catálogo da peça escolhida.</small></span><b>1 crédito</b></p><p><span><strong>Experimentar look com IA</strong><small>Gera a visualização realista do conjunto vestido.</small></span><b>3 créditos</b></p></div></section><section className={styles.history}><span>MOVIMENTAÇÕES</span><h2>Histórico de créditos</h2>{!session?<p>Entre na sua conta para acessar sua carteira.</p>:!entries.length?<p>Ainda não há movimentações. Créditos recebidos de parceiros e utilizados pela IA aparecerão aqui.</p>:entries.map(x=><article key={x.id}><div><strong>{label(x)}</strong><small>{operation(x)}</small></div><b className={x.amount>0?styles.plus:styles.minus}>{x.amount>0?'+':''}{x.amount}</b></article>)}</section></main>}
