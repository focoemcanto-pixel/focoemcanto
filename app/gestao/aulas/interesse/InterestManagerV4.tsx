'use client'

import { useEffect, useMemo, useState } from 'react'
import styles from '../unified-aulas.module.css'

type Lead={id?:string;status?:string;name?:string;whatsapp?:string;modality?:string;availability?:unknown;flexible?:boolean;startIntent?:string;goal?:string;neighborhood?:string;city?:string;createdAt?:string;updatedAt?:string}
type Slot={id:string;day:string;dayOrder?:number;time:string;modality:string;status:string;studentId?:string}
type Match={slot:Slot;score:number;label:string}

const txt=(v:unknown)=>typeof v==='string'?v:''
const norm=(v:unknown)=>txt(v).normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().trim()
const wa=(phone:unknown,msg:string)=>{const raw=txt(phone).trim();const d=raw.replace(/\D/g,'');if(!d)return '#';const p=raw.startsWith('+')?d:(d.startsWith('55')?d:`55${d}`);return `https://wa.me/${p}?text=${encodeURIComponent(msg)}`}
const listAvailability=(v:unknown)=>Array.isArray(v)?v.slice(0,20).map(x=>txt(x).slice(0,120)).filter(Boolean):[]
const period=(time:string)=>{const h=Number((time||'00:00').split(':')[0]);return h<12?'Manhã':h<18?'Tarde':'Noite'}
const statusLabel=(s?:string)=>({waiting:'Na espera',contacted:'Contato feito',offered:'Vaga oferecida',enrolled:'Matriculado',inactive:'Arquivado'}[s||'waiting']||'Na espera')
const hoursSince=(iso?:string)=>{if(!iso)return 0;const n=Date.parse(iso);return Number.isFinite(n)?Math.max(0,Math.floor((Date.now()-n)/3600000)):0}
const daysSince=(iso?:string)=>Math.floor(hoursSince(iso)/24)
function outreachMessage(lead:Lead,best:Match|null,follow:boolean){const first=(txt(lead.name).trim().split(/\s+/)[0]||'').trim();const hi=first?`Oi, ${first}! Tudo bem? 😊`:'Oi! Tudo bem? 😊';const age=daysSince(lead.createdAt);if(follow&&best)return `${hi}\n\nAqui é o Marcos. Passando para saber se você conseguiu ver minha mensagem anterior sobre a vaga de aula individual na ${best.slot.day.toLowerCase()}, às ${best.slot.time}.\n\nSe ainda tiver interesse, me avisa por aqui. Se não fizer mais sentido para você neste momento, sem problema, só me sinaliza para eu liberar a vaga para outra pessoa. 🙏`;
if(age>=180)return `${hi}\n\nAqui é o Marcos, professor de canto. Faz um tempo que você se cadastrou demonstrando interesse nas minhas aulas individuais, e estou atualizando minha lista porque algumas janelas de horário foram abertas.\n\nQueria saber se você ainda tem interesse em fazer o acompanhamento comigo ou se nesse período já conseguiu iniciar com outro professor.\n\nSe ainda tiver interesse, me responde por aqui que eu verifico as possibilidades de horário para você. Se não tiver mais interesse, sem problema também — me avisa só para eu atualizar sua inscrição e liberar a oportunidade para outra pessoa. 😊`;
if(best)return `${hi}\n\nAqui é o Marcos. Você se cadastrou na minha lista de interesse para aulas individuais de canto e surgiu uma possibilidade que pode encaixar no horário que você informou: ${best.slot.day.toLowerCase()}, às ${best.slot.time}, na modalidade ${best.slot.modality.toLowerCase()}.\n\nVocê ainda tem interesse em iniciar? Se sim, me responde por aqui que te explico direitinho como funciona e confirmamos a disponibilidade. 😊`;
return `${hi}\n\nAqui é o Marcos, professor de canto. Você se cadastrou na minha lista de interesse para aulas individuais e estou atualizando as pessoas que ainda desejam iniciar o acompanhamento.\n\nQueria saber se você continua com interesse. Se sim, me responde por aqui e eu verifico as próximas possibilidades de horário para você. Se não tiver mais interesse, sem problema — me avisa só para eu atualizar sua inscrição. 😊`}

function compatibility(lead:Lead,slot:Slot){if(slot.status!=='available'||slot.studentId)return 0;const lm=norm(lead.modality),sm=norm(slot.modality);const modalityOk=sm.includes('online ou presencial')||lm.includes('ambas')||lm.includes('duas')||lm.includes(sm)||sm.includes(lm);if(!modalityOk)return 0;const target=norm(`${slot.day} - ${period(slot.time)}`);let best=0;for(const raw of listAvailability(lead.availability)){const [window='',detail='']=String(raw).split('|').map(x=>x.trim());const nw=norm(window),nd=norm(detail);if(nw===target&&nd===norm(slot.time))best=Math.max(best,100);else if(nw===target&&nd.includes('qualquer'))best=Math.max(best,94);else if(nw===target)best=Math.max(best,82);else if(nw.startsWith(norm(slot.day))&&lead.flexible)best=Math.max(best,66)}if(norm(lead.startIntent).includes('imediat'))best=Math.min(100,best+4);return best}
function matchLabel(score:number){if(score>=95)return 'Encaixe perfeito';if(score>=80)return 'Ótimo encaixe';if(score>=65)return 'Possível encaixe';return 'Sem vaga compatível'}
function priority(lead:Lead,best?:Match|null){let p=best?.score||0;if(norm(lead.startIntent).includes('imediat'))p+=12;if(txt(lead.status)==='waiting'||!txt(lead.status))p+=8;if(txt(lead.status)==='contacted')p+=4;p+=Math.min(10,daysSince(lead.createdAt));if(txt(lead.status)==='offered'&&hoursSince(lead.updatedAt)>=24)p-=8;return Math.max(0,Math.min(120,p))}
function priorityLabel(p:number){if(p>=105)return '🔥 Prioridade máxima';if(p>=90)return 'Alta prioridade';if(p>=70)return 'Boa oportunidade';return 'Acompanhar'}

export default function InterestManagerV4(){
 const [leads,setLeads]=useState<Lead[]>([]),[slots,setSlots]=useState<Slot[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState(''),[filter,setFilter]=useState('active'),[busy,setBusy]=useState('')
 async function load(){setLoading(true);setError('');try{const controller=new AbortController();const timer=window.setTimeout(()=>controller.abort(),8000);const [lr,sr]=await Promise.all([fetch(`/api/admin/aulas?only=leads&t=${Date.now()}`,{cache:'no-store',signal:controller.signal}),fetch(`/api/admin/aulas?only=slots&t=${Date.now()}`,{cache:'no-store',signal:controller.signal})]);window.clearTimeout(timer);if(!lr.ok||!sr.ok)throw new Error('Não foi possível carregar a gestão.');const [ld,sd]=await Promise.all([lr.json(),sr.json()]);setLeads(Array.isArray(ld?.leads)?ld.leads:[]);setSlots(Array.isArray(sd?.slots)?sd.slots:[])}catch(err:any){setError(err?.name==='AbortError'?'A base demorou para responder. Tente novamente.':err?.message||'Erro ao carregar.')}finally{setLoading(false)}}
 useEffect(()=>{load()},[])
 const openSlots=useMemo(()=>slots.filter(s=>s.status==='available'&&!s.studentId),[slots]);const enriched=useMemo(()=>leads.map(lead=>{const matches=openSlots.map(slot=>({slot,score:compatibility(lead,slot)})).filter(x=>x.score>0).sort((a,b)=>b.score-a.score);const best=matches[0]?{...matches[0],label:matchLabel(matches[0].score)}:null;return {...lead,matches,best,priority:priority(lead,best),followup:txt(lead.status)==='offered'&&hoursSince(lead.updatedAt)>=24,offeredHours:txt(lead.status)==='offered'?hoursSince(lead.updatedAt):0}}),[leads,openSlots]);const visible=enriched.filter(l=>filter==='all'?true:filter==='active'?['waiting','contacted','offered',''].includes(txt(l.status)):txt(l.status)===filter).sort((a,b)=>b.priority-a.priority);const perfect=enriched.filter(l=>l.best&&l.best.score>=95&&['waiting','contacted','offered',''].includes(txt(l.status))).length;const followups=enriched.filter(l=>l.followup).length
 async function post(payload:any){const r=await fetch('/api/admin/aulas',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Não foi possível concluir.');return d}
 async function updateStatus(lead:Lead,status:string){if(!lead.id)return;setBusy(`status-${lead.id}`);try{await post({action:'updateLeadStatus',id:lead.id,status});setLeads(v=>v.map(x=>x.id===lead.id?{...x,status,updatedAt:new Date().toISOString()}:x))}catch(e:any){alert(e.message)}finally{setBusy('')}}
 async function deleteLead(lead:Lead){if(!lead.id)return;const name=txt(lead.name)||'este interessado';if(!window.confirm(`Excluir definitivamente ${name}?\n\nEssa ação remove o cadastro da lista de interesse e não poderá ser desfeita.`))return;setBusy(`delete-${lead.id}`);try{await post({action:'deleteLead',id:lead.id});setLeads(v=>v.filter(x=>x.id!==lead.id))}catch(e:any){alert(e.message)}finally{setBusy('')}}
 async function fill(lead:Lead,match:Match){if(!lead.id)return;const modality=match.slot.modality==='Online ou Presencial'?(norm(lead.modality).includes('presencial')&&!norm(lead.modality).includes('online')?'Presencial':'Online'):match.slot.modality;const ok=window.confirm(`Preencher ${match.slot.day}, ${match.slot.time} (${modality}) com ${txt(lead.name)||'este interessado'}?\n\nIsso criará o aluno, ocupará a vaga e marcará o cadastro como matriculado.`);if(!ok)return;setBusy(`fill-${lead.id}`);try{await post({action:'fillSlotFromLead',leadId:lead.id,slotId:match.slot.id,modality});await load()}catch(e:any){alert(e.message)}finally{setBusy('')}}
 return <main className={styles.page}><header className={styles.top}><div><span>FOCO EM CANTO</span><h1>Gestão de Aulas</h1></div><div className={styles.topActions}><a href="/aulasindividuais/" target="_blank">Página pública ↗</a></div></header><div className={styles.shell}><nav className={styles.tabs}><a href="/gestao/aulas/">Agenda</a><a href="/gestao/aulas/alunos/">Alunos</a><a href="/gestao/aulas/interesse/" className={styles.active}>Lista de interesse</a></nav><section style={{display:'grid',gridTemplateColumns:'repeat(4,minmax(0,1fr))',gap:12,marginBottom:22}}><div className={styles.smartStat}><span>Interessados ativos</span><strong>{enriched.filter(l=>['waiting','contacted','offered',''].includes(txt(l.status))).length}</strong><small>Pessoas aguardando oportunidade</small></div><div className={styles.smartStat}><span>Encaixes perfeitos</span><strong>{perfect}</strong><small>Compatibilidade alta com vagas</small></div><div className={styles.smartStat}><span>Follow-ups pendentes</span><strong>{followups}</strong><small>Vagas oferecidas há mais de 24h</small></div><div className={styles.smartStat}><span>Vagas abertas</span><strong>{openSlots.length}</strong><small>Prontas para preenchimento</small></div></section><div className={styles.sectionBar}><div><h2>Lista de interesse</h2><p>Ordenada automaticamente por prioridade comercial e compatibilidade.</p></div></div><div className={styles.smartFilters}>{[['active','Ativos'],['waiting','Na espera'],['contacted','Contato feito'],['offered','Vaga oferecida'],['inactive','Arquivados'],['enrolled','Matriculados'],['all','Todos']].map(([v,l])=><button key={v} className={filter===v?styles.smartFilterActive:''} onClick={()=>setFilter(v)}>{l}</button>)}</div>{loading&&<div className={styles.loading}>Analisando interessados e vagas...</div>}{error&&<div className={styles.error}>{error}<div style={{marginTop:10}}><button onClick={load}>Tentar novamente</button></div></div>}{!loading&&!error&&<section className={styles.smartLeadGrid}>{visible.map((lead,i)=>{const name=txt(lead.name)||'Interessado';const best=lead.best as Match|null;const follow=lead.followup;const msg=outreachMessage(lead,best,follow);return <article className={styles.smartLeadCard} key={txt(lead.id)||`lead-${i}`}><div className={styles.smartLeadTop}><div><div className={styles.smartNameRow}><strong>{name}</strong><span className={`${styles.statusPill} ${styles[`status_${txt(lead.status)||'waiting'}`]||''}`}>{statusLabel(lead.status)}</span><span style={{padding:'5px 8px',borderRadius:999,background:lead.priority>=90?'#332919':'#20262e',color:lead.priority>=90?'#f1cf88':'#aeb6c0',fontSize:9,fontWeight:900}}>{priorityLabel(lead.priority)}</span></div><p>{txt(lead.modality)||'Modalidade não informada'} • {txt(lead.startIntent)||'início não informado'}{lead.createdAt?` • há ${daysSince(lead.createdAt)} dias`:''}</p></div>{best?<div className={styles.matchBadge}><b>{best.score}%</b><span>{best.label}</span></div>:<div className={styles.noMatchBadge}>Sem encaixe agora</div>}</div>{follow&&<div style={{marginTop:12,padding:'10px 12px',borderRadius:12,background:'#2d2117',border:'1px solid rgba(242,180,127,.18)',color:'#f2b47f',fontSize:11,fontWeight:800}}>⚠️ Vaga oferecida há {lead.offeredHours}h sem conclusão. Faça follow-up ou avance para o próximo candidato.</div>}<div className={styles.smartLeadBody}><div><span className={styles.smartLabel}>DISPONIBILIDADE</span><p>{listAvailability(lead.availability).join(' • ')||'Não informada'}</p></div><div><span className={styles.smartLabel}>OBJETIVO</span><p>{txt(lead.goal)||'Não informado'}</p></div></div>{best&&<div className={styles.bestSlot}><div><span>Melhor vaga sugerida</span><strong>{best.slot.day} • {best.slot.time}</strong><small>{best.slot.modality}</small></div><a href="/gestao/aulas/">Ver na agenda ↗</a></div>}<div className={styles.smartActions}>{lead.whatsapp&&<a className={styles.whatsappBtn} href={wa(lead.whatsapp,msg)} target="_blank">{follow?'Fazer follow-up':'WhatsApp'}</a>}{txt(lead.status)!=='contacted'&&txt(lead.status)!=='enrolled'&&txt(lead.status)!=='inactive'&&<button disabled={busy===`status-${lead.id}`} onClick={()=>updateStatus(lead,'contacted')}>Marcar contato</button>}{txt(lead.status)!=='inactive'&&txt(lead.status)!=='enrolled'&&<button disabled={busy===`status-${lead.id}`} onClick={()=>updateStatus(lead,'inactive')}>Arquivar</button>}{txt(lead.status)==='inactive'&&<button disabled={busy===`status-${lead.id}`} onClick={()=>updateStatus(lead,'waiting')}>Restaurar</button>}{best&&txt(lead.status)!=='enrolled'&&txt(lead.status)!=='inactive'&&<button className={styles.fillBtn} disabled={busy===`fill-${lead.id}`} onClick={()=>fill(lead,best)}>{busy===`fill-${lead.id}`?'Preenchendo...':'Preencher vaga'}</button>}<button disabled={busy===`delete-${lead.id}`} onClick={()=>deleteLead(lead)} style={{background:'#35191d',color:'#f1a6ad',border:'1px solid rgba(241,166,173,.18)'}}>{busy===`delete-${lead.id}`?'Excluindo...':'Excluir'}</button></div></article>})}{!visible.length&&<div className={styles.bigEmpty}>Nenhum cadastro neste filtro.</div>}</section>}</div></main>
}
